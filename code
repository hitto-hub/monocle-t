import random
import display
import time
import touch
display.brightness(3)
def fn(arg):
    global f
    print(touch.state())
    if f == 0:
        quiz()
    if f == 2:
        global sum
        global f
        f = 0
        sum = str(sum)
        text = display.Text(sum, 320, 200, display.WHITE, justify=display.MIDDLE_CENTER)
        display.show(text)
def bold(num, sec):
    width = 1
    timeout = time.time() + sec
    while True:
        text = display.Text(num, 320, 200, display.WHITE, justify=display.MIDDLE_CENTER)
        display.show(text)
        text = display.Text(num, 320 + width, 200, display.WHITE, justify=display.MIDDLE_CENTER)
        display.show(text)
        text = display.Text(num, 320, 200 + width, display.WHITE, justify=display.MIDDLE_CENTER)
        display.show(text)
        text = display.Text(num, 320 + width, 200 + width, display.WHITE, justify=display.MIDDLE_CENTER)
        display.show(text)
        if time.time() >= timeout:
            break
def quiz():
    global sum
    global f
    global tmp
    sum = 0
    f = 1
    bold("start!", 2)
    for i in range({num}):
        while True:
            num = int(random.randint({min}, {max}))
            if num != tmp:
                tmp = num
                break
        sum += num
        num = str(num)
        bold(num, {int})
    text = display.Text('button press is answer', 320, 200, display.WHITE, justify=display.MIDDLE_CENTER)
    display.show(text)
    f = 2
f = 0
tmp = 0
text = display.Text('button press is start', 320, 200, display.WHITE, justify=display.MIDDLE_CENTER)
display.show(text)
touch.callback(touch.EITHER, fn)
